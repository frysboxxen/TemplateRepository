# TemplateRepository
Contains files good to use in a new repository or project.
Copy the text from .gitignore file to every new repository. It automatically detects .class fils, windows and mac system files. And doesn't upload them.


TODO:
- Add all Std... files, In.java and Out.java
